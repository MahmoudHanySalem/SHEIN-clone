import { ShortenNumberPipePipe } from './shorten-number-pipe.pipe';

describe('ShortenNumberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenNumberPipePipe();
    expect(pipe).toBeTruthy();
  });
});
